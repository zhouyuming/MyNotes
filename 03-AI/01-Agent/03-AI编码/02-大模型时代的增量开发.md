# 大模型时代的增量开发

在当前主流实践中，大模型辅助开发大致有两种典型模式：

Vibe Coding模式：通过自然语言描述需求，让模型一次性生成页面/模块代码，再由人工手动调整。特点是上手快、反馈快，但缺乏结构化约束，代码质量高度依赖prompt水平与模型状态。

片段补全模式：在IDE中由模型补全函数、生成样板代码、写单测等。 特点是贴近传统开发流程，但对整体架构与演化路径的把控较弱。

这两种模式有一个共同点：都更偏向从0到1的生成，而不是在既有系统中做增量演化。

## 增量开发为什么更难？

从工程视角看，增量开发与全新开发的本质差异在于全新开发只需要在“需求→设计→实现”这条单向链路上做正确的事情，历史包袱几乎为零。而增量开发必须在“历史约束→当前需求→未来演化”三者之间找到平衡。具体到大模型参与的场景，增量开发的难点主要体现在：

风格一致性难以保证

隐含约束难以显式化

演化路径难以规划

##  为什么要引入规格驱动开发（Spec-Driven Development, SDD）？

在SDD中，我们用结构化规格（Spec）来约束模型与人类的协作边界。在大模型参与开发的场景下，SDD 的价值主要体现在：

- 把隐含约束显式化： 通过OpenSpec生成的spec.md、design.md、task.md等，把原本散落在代码、脑海、会议纪要中的约束，转化为模型可消费的结构化输入。
- 把一次性对话升级为可复用资产：Prompt是一次性的，而Spec是可版本化、可演化、可复用的。
- 把模型输出纳入工程闭环：模型不再直接对接代码仓库，而是先对接 Spec，再由 OpenCode 将 Spec 映射为具体代码变更。

## 实践方法：基于OpenCode与OpenSpec的协同开发

### 整体视角

在这次实践中，我们把“人–模型–工具链”之间的关系抽象为一个三层协同框架：

- 意图层（Human Intent）：最上层是人类开发者的“意图层”，这是所有开发活动的起点，也是唯一能够理解业务目标、体验要求、历史包袱和未来演化方向的主体。意图层的表达往往是自然语言的，但自然语言本身是模糊的、带有歧义的，也无法直接作为模型的输入。模型需要的是一种更明确、更结构化的表达方式，而这正是规格层存在的意义。
- 规格层（OpenSpec/SDD）：通过OpenSpec将意图转化为结构化规格，包括spec.md、design.md和task.md。这些文档并不是传统意义上的“需求文档”，而是模型与人类之间的契约。spec.md描述功能与行为约束；design.md描述架构与交互设计；task.md则把需求拆解为可执行的开发任务。
- 实现层（OpenCode + GLM4.7）：OpenCode 的作用并不是简单地“调用模型”，而是把规格层的任务映射到具体的代码文件和上下文片段中，让模型在正确的位置、基于正确的历史代码进行增量修改。而GLM4.7是一个遵循规则的执行者。

### 协作模式

我们划分了两种协作模式：

- 从0到1（模型主导，规格辅助）：适用于新模块、新页面、新服务；Spec更偏向“约束边界”与“非功能性要求”。
- 从1到N（规格主导，模型受控）：适用于增量开发、功能扩展、行为调整；Spec需要显示描述与现有页面/模块的关系，需要参考或复用的模式，不允许破坏的行为契约等。

## SDD驱动的增量开发流程设计

### 阶段1：从自然语言到结构化Spec

### 阶段2：规格审阅与隐含约束补全

### 阶段3：任务拆解与OpenCode驱动

核心：让任务成为连接Spec与代码的中间层，而不是让模型直接在代码上自由发挥。

### 阶段4：大模型在约束下执行增量变更

### 阶段5：行为验证与对齐

### 阶段6：回写Spec，让规格与实现共同演化

## 有效性分析

### 差异分析

- 沟通成本的变化
- 代码演化轨迹的变化
- 心智负担的变化

SDD并没有降低对工程能力的要求，反而提高了对架构能力和系统思维的要求

当系统的隐含约束过多、历史包袱过重时，模型很容易做出技术上正确但业务上错误的修改。这意味着在更复杂的场景中，人类仍然需要承担大量的审查、判断和兜底工作。

在具体执行时，一定要扭转“先写代码”的惯性，坚持规格（Spec）先行。要把Spec当成是你的架构思考过程，而不是额外增加的文档负担。在动手撸代码前，先让AI帮你盘一遍Spec里的边界条件和逻辑漏洞，这能帮你省去后期大量的Debug时间。

### 团队层面的思考

团队需要重新定义“文档”的价值。Spec不再是可有可无的说明书，而是核心研发资产。管理者应当把OpenSpec或OpenCode类的工具看作是协作的基础设施，而非单纯给个人提效的插件。

最关键的认知转变是：把AI定位成“有约束的执行者”。 它不是闭着眼写代码的生成器，而是在Spec约束下干活的“高级外包”。当团队习惯了通过Spec与AI协作，大家的精力才会真正从“怎么写代码”转移到“怎么设计系统”上。

## 总结与展望

从工程视角看，大模型时代的软件开发正在从“如何更快写出代码”转向“如何让代码在一个复杂系统中长期健康地演化”。

SDD + OpenSpec + OpenCode提供了一种新的协作方式：让模型在规格约束下参与系统演化，让人类从“写代码的人”变成“设计规则的人”。

将Spec与测试进一步打通，形成“规格–测试–实现”的TDD闭环；通过增加TDD环节，探索更自动化地驱动代码生成与回归测试，减少规格偏离风险。
