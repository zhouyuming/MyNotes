# AGENTS.md - MyNotes 仓库指南

## 仓库概述

MyNotes 是一个个人知识库笔记项目，采用 Markdown 格式编写，包含以下主要分类：

- **01-工具使用** - 开发工具使用总结
- **02-投资理财** - 投资理财相关分析
- **03-AI** - AI/Agent 相关技术笔记
- **04-Coding** - 编程相关资源
- **AI** - AI 相关工具使用
- **嵌入式** - 嵌入式开发笔记
- **编程语言与编译构建** - 编程语言学习与构建工具
- **编译构建** - 构建系统深入学习
- **芯片与硬件** - 硬件相关笔记
- **设计模式** - 设计模式学习

## 目录结构规范

```
MyNotes/
├── 01-工具使用/
├── 02-投资理财/
├── 03-AI/
├── 04-Coding/
├── AI/
├── 嵌入式/
├── 编程语言与编译构建/
├── 编译构建/
├── 芯片与硬件/
├── 设计模式/
├── README.md
└── AGENTS.md
```

### 命名规范

- 文件名使用中文描述主题
- 多级文件使用序号前缀（如 `01-`、`02-`）
- 每个分类目录应包含 `README.md` 作为索引
- 技术笔记按主题分组，不按日期分组

## Markdown 编写规范

### 文件头

```markdown
# 标题

简要描述（1-2行）

[目录链接]
```

### 内容结构

- 使用 `##` 作为一级标题，`###` 作为二级标题
- 保持段落简洁，每段不超过 5-6 行
- 代码块必须标注语言类型

```markdown
​```python
def example():
    pass
​```
```

### 列表使用

- 无序列表使用 `-` 或 `*`
- 有序列表使用 `1.` `2.` `3.`
- 嵌套列表不超过 3 层

### 链接和图片

- 相对路径优先于绝对路径
- 内部链接使用相对路径：`[标题](./path/file.md)`
- 图片放在同目录或 `images/` 子目录

### 表格

```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| 内容 | 内容 | 内容 |
```

## 工具集成

### Mermaid 图表

仓库支持 Mermaid 语法绘制图表：

```markdown
​```mermaid
graph TD
    A --> B
    B --> C
​```
```

详见 `01-工具使用/03-Mermaid/` 目录。

### Mermaid 支持

- GitHub 支持原生 Mermaid 渲染
- VS Code 推荐安装 Mermaid 插件

## Git 工作流

### 提交规范

使用简洁的中文描述：

```
git commit -m "添加 01-XXX 笔记"
git commit -m "更新 02-YYY 内容"
git commit -m "修正 03-ZZZ 错别字"
```

### 分支策略

- 主分支：`main` - 生产内容
- 工作分支：`work/` - 新增内容
- 修改分支：`fix/` - 错误修正

### 常用命令

```bash
# 克隆仓库
git clone git@github.com:zhouyuming/MyNotes.git

# 创建新分支
git checkout -b work/新笔记名称

# 添加更改
git add .
git commit -m "描述"

# 推送更改
git push -u origin HEAD
```

## 笔记维护规范

### 新增笔记

1. 确定分类目录
2. 创建 Markdown 文件
3. 更新同级目录的 `README.md` 索引
4. 如有必要，更新根目录 `README.md`

### 内容更新

1. 保持内容准确性和时效性
2. 添加更新日期或版本信息（可选）
3. 检查相关链接是否有效

### 错误修正

1. 创建 `fix/` 分支
2. 修正错误
3. 提交并合并到主分支

## AI/Agent 相关

仓库包含 AI Agent 相关技术笔记，详见：

- `03-AI/01-Agent/` - Agent 基础与实践
- `03-AI/02-Agen实战项目/` - 实战案例
- `AI/01-OpenClaw/` - OpenClaw 工具使用

## 常见任务

### 添加新分类

1. 创建目录 `XX-分类名称/`
2. 添加 `README.md` 索引文件
3. 更新根目录 `README.md`

### 添加新笔记

1. 确定所属分类
2. 创建文件：`序号-标题.md`
3. 编写内容
4. 更新 `README.md` 索引

### 搜索内容

```bash
# 搜索关键词
grep -r "关键词" . --include="*.md"
```

## 注意事项

- 所有笔记使用中文编写
- 保持格式一致性
- 定期检查链接有效性
- 重要内容添加标签或高亮
